'use strict';

/**
 * @ngdoc module
 * @name appController
 * @description
 *  A module to hold all the controllers used in the app
 *  
 *  ## Dependencies
 *  Requires that {@link appModel 'appModel'}, {@link appService 'appService'} and {@link ngMaterial 'ngMaterial'} be initialized first
 */
angular.module('appController', ['appModel', 'appService', 'ngMaterial']);