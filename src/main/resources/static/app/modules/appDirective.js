'use strict';

/**
 * @ngdoc module
 * @name appDirective
 * @description
 *  A module to hold all the custom directives used in the app
 *  
 *  ## Dependencies
 *  Requires that {@link appService 'appService'} and {@link appFilter 'appFilter'} be initialized first
 */
angular.module('appDirective',['appService', 'appFilter']);
