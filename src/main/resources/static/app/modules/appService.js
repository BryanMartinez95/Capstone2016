'use strict';

/**
 * @ngdoc module
 * @name appService
 * @description
 *  A module to hold all the services used within the app
 *  
 *  ## Dependencies
 *  Requires the {@link appModel 'appModel'} module be initialized first
 */
angular.module('appService', ['appModel']);